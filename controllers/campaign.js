//var campaigns = require('../services/campaign-service')

exports.create = async (req, res) => {
    // var minimum_contribution = req.body.minimum_contribution
    // var account_id = req.body.account_id
    // await campaigns.create(minimum_contribution, account_id)
    // res.status(200).send('10',)
}